# تقرير ما قبل الإطلاق — سوق برطعة
**التاريخ:** 20 آب 2026 · **لمالك المنصة** · (هذا تقرير للمالك بالعربي — وثائق الكود تظل بالإنجليزي حسب سياسة المشروع)

---

## 1. الوضع العام بجملة واحدة

المنصة متقدمة جداً ومنضبطة بشكل نادر: 9 مراحل مكتملة، 76 ملف اختبار (فوق 1000 اختبار)، صفر TODO/console.log بالكود، وكل "هيكل غير مكتمل" موثّق سببه. الجاهزية للإطلاق مش ناقصها بناء كبير — ناقصها **تشغيل الفحوصات النهائية، إغلاق قائمة محددة من الثغرات المنطقية، وأعمال سيرفر** ما إلها كود.

---

## 2. غير منجز — لازم يخلص قبل الإطلاق (بالأولوية)

### 2.1 فحوصات المرحلة 9 ما اشتغلت كاملة ⚠️ (الأهم)
بيئة الفحص السابقة ما قدرت تشغّل المتصفح ولا ESLint، فبقيت هاي البنود مفتوحة رسمياً:
- `pnpm lint` كامل — ما ركض.
- `pnpm test` كامل — ركض ملف واحد من 49. **اختبار المخزون المتزامن (stock_qty) ما انفّذ ولا مرة.**
- `pnpm e2e` كامل + axe على القوالب الخمسة + قياس سرعة LCP — كلها بدها متصفح.
- `pnpm db:seed` — بياناته متحققة، تنفيذه لأ.

**المطلوب منك:** تشغيلها على جهازك (أو أعطيني جلسة على الجهاز نفسه وبشغلها وبصلّح أي كسر):
```
pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm e2e
```

### 2.2 ولا اختبار e2e للمرحلة 9
كل مرحلة سابقة إلها ملف e2e — المرحلة 9 (المتغيرات، المخزون، مناطق التوصيل، البنرات، CRM، البحث، القالبين الجديدين) ما إلها ولا واحد. لازم ملف `phase9-*.spec.ts` قبل الإطلاق.

### 2.3 طلبات الديمو العامة ما بتنبّه حدا 💰 (خطر ضياع مبيعات)
زبون محتمل يعبي نموذج طلب الديمو → ينحفظ الطلب و**ما توصلك أي رسالة**. الحدث `demo_request.received` معرّف بالكود وما بينبعث أبداً، وجدول الإشعارات بيشترط tenantId فما بينفع لإشعار منصة. الحل المقترح: انبعاث الحدث → n8n يبعثلك واتساب، + إشعار بلوحة الإدارة. (تعديل بسيط + migration صغيرة).

### 2.4 بوابات الدفع الإلكتروني (مقصود، بس لازم تعرفه)
الشغال الوحيد: تحويل يدوي/كاش. Meshulam وTranzila وPayPal هياكل غير مفعّلة عمداً (بدها كيان مسجّل) — وPayPal تحديداً بدها إعادة كتابة للتحقق مش بس تفعيل. **مربوطة بقرار موثّق:** لما تفعّل أول بوابة حقيقية، لازم ترفع النسخ الاحتياطي من "كل 6 ساعات" إلى أرشفة WAL لحظية — 6 ساعات مقبولة لتعديلات منتجات، مش لدفعات مقبوضة.

### 2.5 أعمال سيرفر (أول مهام التشغيل — الكود جاهز إلها)
- staging مطابق + **تجربة استرجاع حقيقية** من نسخة مشفرة (البند الوحيد المعلّم "متعمد غير مكتمل" بالمرحلة 7).
- تركيب قاعدة R2 lifecycle لحذف النسخ بعد 14 يوم (السكربت بيفحصها وبيشتكي — ما بيقدر يركّبها).
- تقييد CDN على مجلد الصور فقط + توكن Cloudflare لمسح الكاش.
- الدليل الكامل جاهز: `docs/DEPLOY.md`.

### 2.6 "يوم التاجر الكامل" اليدوي
موجود بالخطة وما انعمل: إنشاء حساب → رفع منتجات → تغيير ألوان → طلب واتساب → تعليق الاشتراك → تنزيل النسخة من الرابط → استرجاع → التأكد إن النسخة انحذفت.

---

## 3. ثغرات منطقية وتحسينات تجربة — مرتبة بالأثر

| # | المشكلة | الأثر | الحجم |
|---|---------|-------|-------|
| 1 | **صندوق الطلبات المدمج**: لما تفعّل السلة لتاجر، شاشة الطلبات بتعرض طلبات السلة فقط — طلبات "اطلب الآن" القديمة بتختفي | تاجر بيفقد نصف تاريخه | متوسط |
| 2 | **تصدير التاجر ناقص حقول السلة**: orders.csv بدون قناة الطلب، رسوم التوصيل، الكوبون، العنوان، كود التتبع | تصدير غير كامل | صغير |
| 3 | **إعدادات الطلبات المقفلة** ما فيها زر «اطلب تعديل» مثل باقي الحقول المقفلة | طريق مسدود للتاجر | صغير |
| 4 | **اللون المخصص**: «لون السطح» بينسخ من الخلفية تلقائياً إذا التاجر ما انتبهله — الكروت بتذوب بالخلفية | تصاميم أقل جودة | صغير |
| 5 | **نظامين إحصائيات بالتوازي**: Umami القديم + تحليلات الطرف الأول الجديدة — شاشتين بالداشبورد لنفس السؤال | إرباك للتاجر | قرار + متوسط |
| 6 | **عمود demoPackKey ناقص**: قائمة الديمو بتعرض القالب مش الباقة اللي انبنى منها | تقارير مبيعات أضعف | صغير + migration |
| 7 | **ما في إخفاء هوية تلقائي لبيانات زبائن الطلبات** (اسم/هاتف/عنوان) بعد مدة احتفاظ | فجوة خصوصية مسجلة | ميزة كاملة |
| 8 | **رفع الصور** بيستخدم إدخال طابور بلا مهلة — إذا Redis واقع بيعلق الرفع بلا سقف (باقي الأماكن مضبوطة 5 ثواني) | تعليق نادر | صغير جداً |
| 9 | **CSS مزدوج** لقالبي bayt/raff (مستورد مرتين — مرة بـ layout ومرة بـ @import) | بايتات زايدة + ترتيب cascade | صغير جداً |
| 10 | **تنظيفات**: كلاس خطأ ميت `NotImplementedInPhaseError`، تعليقات قديمة مضللة (features.ts بيحكي 6 قدرات وهنّ 15؛ رأس schema.prisma بيدّعي ولا جدول بعد المرحلة 5) | تضليل المطور الجاي | صغير جداً |
| 11 | **قيد ناقص بقاعدة البيانات**: «بوابة واحدة مفعلة لكل تاجر» قاعدة تطبيق فقط — partial unique index حزام أمان | حماية إضافية | صغير + migration |
| 12 | **زر «الدفع عبر البوابة» بالسلة** تسمية فقط — ما بينشئ رابط دفع (منطقي حالياً: ما في بوابة مفعلة) | ينوثّق ويُربط لاحقاً | مع تفعيل البوابات |

**موثّق ومقبول (ما بنصح نلمسه هلق):** CSP فيها unsafe-inline (قيد تقني بـ Next 16.3 — نراقب التحديثات)، سباق نادر بحد الكوبون لكل هاتف، عدّ ناقص بدفعة إشعارات فاشلة، `/internal/domain-ask` بدون سر مشترك (الحماية شبكة داخلية + rate limit).

---

## 4. قرارات مفتوحة إلك (مش مستعجلة — بس قرّرها)

1. **توحيد الإحصائيات:** اقتراحي نوحّد التاجر على النظام الجديد (الطرف الأول) ونخلي Umami أداة داخلية للمالك فقط — يختفي الازدواج من الداشبورد.
2. **مدة احتفاظ بيانات زبائن الطلبات:** اقتراحي سنتين ثم إخفاء هوية تلقائي (الاسم/الهاتف/العنوان بيصيروا «زبون محذوف» والأرقام المالية بتظل). قابلة للضبط لكل تاجر أو موحّدة؟
3. **ترخيص الحزمة المستقلة:** لما تصدّر موقع لعميل، الحزمة فيها كود المنصة — بدك عقد/رخصة بسيطة مرفقة؟ (قرار تجاري مش تقني).

---

## 5. الميزتان الجديدتان — حسب قراراتك اليوم (المرحلة 10)

المواصفات التقنية الكاملة بالإنجليزي: `docs/PHASE-10.md`. الخلاصة بالعربي:

### أ. شاشة النسخ الاحتياطي — للمالك فقط
- مسار `/backups` بلوحة الإدارة (سطح admin.* منفصل أصلاً — التاجر ما بيوصله ولا بيشوفه).
- بتعرض: كل جولات النسخ من التخزين السحابي، آخر نسخة ناجحة وعمرها، الأحجام، حالة قاعدة الحذف التلقائي، وتحذير أحمر إذا آخر نسخة أقدم من الفترة المضبوطة.
- زر **«انسخ الآن»**: بيبعث أمر عبر Redis لحاوية النسخ فبتنفّذ جولة فورية (حاوية الويب ما فيها أدوات pg_dump عمداً — هيك بنحافظ على العزل). كل كبسة مسجلة بسجل تدقيق المنصة.
- **تنزيل** النسخة المشفرة برابط موقّع قصير العمر. بتظل مشفرة — مفتاح فك التشفير عندك خارج السيرفر (هذا التصميم الصحيح وما منغيره).
- **الاسترجاع بيظل من السيرفر** حسب الدليل — والشاشة بتعرض خطواته. (قرارك: بدون زر استرجاع بالواجهة).

### ب. نسخة التاجر — للمالك فقط
- تبويب «النسخ» بصفحة حساب كل تاجر: **إنشاء نسخة متجر** = ملف واحد فيه كل بيانات المتجر (منتجات، أقسام، طلبات، إعدادات، تصميم، مناطق توصيل، زبائن...) + صوره + بصمة إصدار المخطط.
- **استرجاع متجر واحد** من النسخة بدون لمس باقي التجار — مع إيقاف مؤقت لمهام المتجر أثناء الاسترجاع (نفس نمط الحذف الآمن الموجود).
- محفوظة مشفرة تحت مجلد التاجر، خارج CDN وخارج عداد تخزينه، وبتنحذف معه إذا انحذف (منسجم مع وعود الخصوصية المنشورة).

### ج. التصدير المستقل — حزمة تشغيلية كاملة، للمالك فقط
- زر «تصدير موقع مستقل» بصفحة حساب التاجر، بيبني ملف مضغوط فيه:
  1. نسخة التاجر (ب أعلاه)،
  2. كود المنصة (بدون أي أسرار أو ملفات .env)،
  3. docker-compose مبسّط (web + worker + postgres + redis + caddy)،
  4. سكربت تنصيب واحد: يبني القاعدة → يستورد النسخة → يرفع الصور لتخزينه الخاص → ينشئ حساب مالك جديد بكلمة سر لمرة واحدة،
  5. تعليمات تشغيل بالعربي.
- وضع جديد بالكود `SINGLE_TENANT_MODE`: الدومين بيقدّم المتجر مباشرة، لوحة التاجر شغالة، **بدون** لوحة مالك وبدون نظام خطط (كل الميزات مفتوحة).
- **بصراحة:** هذا أكبر جزء بالشغل — أكبر من (أ) و(ب) مجتمعتين، لأن المنصة مبنية بعمق على تعدد التجار. مقسّم بالمواصفات على مسارات مستقلة قابلة للتسليم واحدة واحدة.

---

## 6. خطة التنفيذ المقترحة

1. **فحوصات المرحلة 9 كاملة** على جهاز حقيقي + إصلاح أي كسر (البند 2.1) — قبل أي شغل جديد.
2. **حزمة إصلاحات ما قبل الإطلاق** (البنود 2.3 + جدول القسم 3: 1، 2، 3، 4، 8، 9، 10، 11) — يوم-يومين شغل.
3. **المرحلة 10أ:** شاشة النسخ + نسخة التاجر.
4. **المرحلة 10ب:** التصدير المستقل.
5. **أعمال السيرفر** (2.5) لما تجهّز البنية — وبعدها «يوم التاجر الكامل» (2.6) وبتطلق.

كل بنود هذا التقرير انضافت كمربعات متابعة بآخر `TODO.md` (قسم Pre-launch fixes + قسم Phase 10).

---

## 7. تحديث — 20 آب 2026 مساءً: حزمة الإصلاحات نُفّذت

انكتب الكود لكل بنود «الإصلاحات السريعة» (10 إصلاحات): إشعار طلبات الديمو، صندوق الطلبات المدمج،
حقول السلة بالتصدير، زر «اطلب تعديل» بإعدادات الطلبات، لون السطح المستقل، رفع الصور بمهلة زمنية،
حذف الـ CSS المزدوج، عمود demoPackKey + عرض الباقة بقائمة الديمو، قيد «بوابة واحدة مفعلة»،
والتنظيفات — مع migration واحدة جديدة وتحديث `TODO.md` و`docs/DECISIONS.md`.

**مهم:** بيئة هذه الجلسة ما فيها أدوات التشغيل، يعني الكود مكتوب ومراجَع يدوياً لكن **ما انفحص آلياً
بعد**. أول خطوة إلزامية على جهازك، بهذا الترتيب:

```
pnpm db:migrate
pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm e2e
```

إذا طلع أي خطأ — انسخه وابعثلي إياه بجلسة على نفس الجهاز وبصلّحه.

---

## 8. تحديث ثانٍ — المرحلة 10 انبنت كمان

بنيت الميزتين اللي طلبتهم كاملات (المواصفات بـ `docs/PHASE-10.md`):

**أ. شاشة النسخ الاحتياطية** — `/backups` بلوحة المالك: كل جولات النسخ وأحجامها، آخر نسخة ناجحة
وعمرها بالأحمر إذا تأخرت، حالة قاعدة الحذف التلقائي، زر «خُد نسخة الآن» (بيوصل خدمة النسخ خلال دقيقة)،
وتنزيل أي نسخة مشفّرة. خطوات الاسترجاع معروضة على الشاشة نفسها بدون زر تنفيذ — عن قصد.

**ب. نسخة التاجر** — تبويب «النسخ» بصفحة كل حساب: خُد نسخة، نزّلها، رجّعها (بتأكيد كتابة اسم المتجر)،
أو احذفها. الاسترجاع **ما بيلمس** الاشتراك والدفعات ومفاتيح الدفع وسجل العمليات — وهذا مكتوب على الشاشة
مش بس بالكود.

**ج. التصدير المستقل** — زر «جهّز نسخة مستقلة» بينتج ملف واحد فيه: كود الموقع، بيانات المتجر وصوره،
ملفات تشغيل جاهزة (docker-compose + Caddy)، سكربت تنصيب بأمر واحد، وشرح عربي كامل لصاحب السيرفر.
المميزات بتنثبّت على وضع المتجر يوم التصدير.

**مراجعة ذاتية:** بعد ما خلصت البناء شغّلت مراجعة عدائية على الكود الجديد، طلعت 9 ملاحظات — صلّحت
كلها، منها ٣ حقيقية ومهمة: خطأ بتصدير الصور كان بيخلي ٥ من كل ٦ روابط صور مكسورة بعد الاسترجاع،
وثغرة قديمة (من مرحلة سابقة) بإلغاء المهام قبل الحذف النهائي، وادّعاء غير دقيق بالتوثيق عن العزل بالنسخة
المستقلة — صار مكتوب بصراحة مع شرح متى بيصير خطر.

**نفس التحذير:** كل هذا مكتوب ومراجَع، **وما انفحص آلياً**. في migration ثانية جديدة
(`20260821000000_phase10_backups`) لازم تنطبّق. الأمر واحد:

```
pnpm db:migrate
pnpm typecheck && pnpm lint && pnpm test && pnpm build && pnpm e2e
```

الباقي بعد ما يخضرّ: اختبار e2e للمرحلة 9 والمرحلة 10، «يوم التاجر الكامل» اليدوي، وأعمال السيرفر (2.5).

---

## 9. تحديث — 20 آب 2026، 2:00 ظهراً: **الموقع اشتغل فعلياً**

شغّلت المنصة على جهازك وتحققت من المرحلة 10 بالمتصفح. النتائج:

**اشتغل:**

| الفحص | النتيجة |
|---|---|
| `20260820000000_pre_launch_fixes` | ✅ انطبّقت |
| `20260821000000_phase10_backups` | ✅ انطبّقت |
| `prisma generate` + `db:seed` | ✅ |
| Next.js 16.3 | ✅ جاهز خلال ٧٫٨ ثانية |
| لوحة المالك `/` | ✅ 200 |
| **`/backups` (المرحلة ١٠أ)** | ✅ 200 — بتعرض «مش مربوطة بعد» صح، وخطوات الاسترجاع كاملة |
| **`/accounts/{id}/backups` (المرحلة ١٠ب)** | ✅ 200 — «خُد نسخة الآن» و«جهّز نسخة مستقلة» ظاهرين |

**السبب الحقيقي اللي كان موقّف كل شي:** Docker. هذا الجهاز **بيعمل شاشة زرقاء** (bugcheck 0x7E) لما Docker Desktop يشغّل محرك WSL2 — موثّق بـ `bsod-evidence.txt` و`restart-diagnosis.txt`. كل سكربتاتي السابقة كانت مبنية على Docker، فما كان لها أي فرصة تشتغل.

**الحل:** في سكربت `scripts/dev-native.ts` موجود من جلسة سابقة بيشغّل كل شي **بدون Docker نهائياً** — postgres مدمج بالعملية نفسها، بلا redis (كل شي بيرجع لقاعدة البيانات)، وبريد داخلي. عملتلك ملف `START-HERE.cmd` بجذر المشروع بيشغّله بدبل-كلك.

**ملاحظتان غير مؤذيتين ظهرتا:**
- تحذير hydration mismatch سببه **إضافات المتصفح** (MegaBonus وBitdefender بيحقنوا عناصر بالصفحة) — مش خلل بالمشروع.
- `eval() is not supported` — هذا نظام CSP الصارم من المرحلة ٦ شغّال صح؛ رسالة تطوير فقط.

**ملف `dev-up.cmd` القديم (مبني على Docker) ما عاد له داعي على هذا الجهاز.** استعمل `START-HERE.cmd`.

**لسا مطلوب:** `pnpm typecheck` و`pnpm lint` و`pnpm test` كاملين — الصفحات اللي فتحتها انترجمت بنجاح، بس Next بترجم الصفحة لما تفتحها فقط.

---

## 10. فحص QA كامل — 21 آب 2026

شغّلت الفحوصات الآلية وتصفّحت كل أركان المنصة فعلياً. الملف الكامل: `qa-report.log`.

### الفحوصات الآلية

| الفحص | النتيجة |
|---|---|
| `pnpm typecheck` | ✅ **صفر أخطاء** |
| `pnpm lint` | ✅ **صفر أخطاء** |
| `pnpm test` | ⚠️ اشتغل جزئياً — انقطع بسبب قيد بالجهاز (تحت) |

**قبل الإصلاح كان في ٥ أخطاء — صلّحتها كلها:**

- **خطأان بالأنواع**، سببهما تعديلي على «لون السطح» (خلّيته يقبل «فارغ»). الملفان `b3-demo.test.ts` و`phase9-templates.test.ts` كانا بيفترضا إنه مش فارغ أبداً. صلّحتهم بطريقة **بتقوّي الاختبار مش بتسكّته**: صاروا يتحققوا إنه المجموعات الجاهزة والقوالب **لازم** يكون عندها لون سطح صريح — وهذا بالضبط الانحدار اللي تعديلي كان ممكن يسبّبه.
- **٣ أخطاء lint قديمة** من المرحلة ٩ (مش من شغلي):
  - `search/page.tsx` — متغيّر `searchParams` مستلَم وما بينستعمل. شِلته، وكتبت ليش: وضع كلمة البحث بعنوان الصفحة معناه عكس نص يكتبه الغريب داخل `<title>` ووسوم المشاركة.
  - `phase9-towns.test.ts` — «مسافات غير اعتيادية». **هون كان الفخ:** هدول الأحرف المخفية **هي الاختبار نفسه** (بتتأكد إنه اسم بلدة منسوخ من واتساب بتنشال منه علامات الاتجاه). حذفهم كان رح يفرّغ الاختبار من معناه — فحطيت استثناء مكتوب سببه.

### تصفّح فعلي لكل الأركان

| السطح | الصفحات | النتيجة |
|---|---|---|
| لوحة المالك | نظرة عامة · الحسابات · صفحة حساب · دورة الاشتراك · النسخ التجريبية · الباقات · الخصوصية · **النسخ الاحتياطية** · **نسخ الحساب** | ✅ كلها 200 |
| المتجر | الرئيسية · المنتجات · sitemap · سياسة الخصوصية · بوابة الديمو | ✅ |
| لوحة التاجر | الرئيسية · الطلبات · إعدادات الطلبات · المظهر | ✅ |

**اختبارات وظيفية نجحت:**
- **أنشأت نسخة تجريبية جديدة من الصفر** → انبنى متجر بـ ١٥ منتج، وعمود «نوع النشاط» طلّع **«ملابس وأزياء»** — يعني عمود `demoPackKey` والـ migration تبعته شغّالين فعلياً، والنسخ القديمة ظلّت تعرض «ما حدّد» وهذا التصرّف الصحيح.
- **الدخول باسم التاجر** من لوحة المالك اشتغل — يعني تسليم الجلسة بين النطاقين سليم.
- **بوابة الديمو**: فتح متجر تجريبي بلا رمز → صفحة الرفض بالعربي. مع الرمز → المتجر كامل.
- **سياسة الخصوصية المولّدة** دقيقة: عرفت إنه السلة مفعّلة وكتبت بنود بيانات الطلب، وذكرت النسخ الاحتياطية بصراحة.

### قيود الجهاز (مش أخطاء بالمشروع)

1. **الاختبارات ما بتكمّل على هذا الجهاز.** قاعدة البيانات المدمجة (نسخة beta) بتنهار على ويندوز لما تنفتح اتصالات كثيرة بوقت واحد — وهذا موثّق أصلاً بـ `scripts/dev-native.ts`. مرّت ٣٤/٣٤ باختبار الصور قبل ما تنقطع. **الحل:** شغّلها على CI، أو ركّب PostgreSQL عادي وحُط `DATABASE_URL_TEST` بملف `.env`.
2. **منفذ الاختبارات كان محجوز.** الافتراضي 55432 واقع داخل مدى بيحجزه Hyper-V/WSL، فقاعدة البيانات ما كانت تقدر تبدأ أصلاً وvitest كان بيقول «ما في ملفات اختبار». حطيت `EMBEDDED_PG_PORT=5433` بملف `QA-CHECK.cmd`.

### ملاحظات صغيرة

- `sitemap.xml` بيكتب الروابط بلا رقم المنفذ (`...localhost/` مش `...localhost:3000/`). **صح بالإنتاج** لأنه ما في منفذ، وبيظهر بس بالتطوير. ما غيّرته عمداً — تعديل كود SEO لأجل شكل بالتطوير مخاطرة بلا مقابل.
- تحذير hydration بالمتصفح سببه إضافاتك (MegaBonus / Bitdefender بيحقنوا عناصر). مش من المشروع.

**الخلاصة:** المنصة جاهزة من ناحية الكود — كل الأسطح بتشتغل، والفحص الثابت نظيف تماماً. الباقي الوحيد هو تشغيل حزمة الاختبارات على بيئة بتتحمّلها.

---

## 11. جلسة 21 آب — شريط إتقان تك + فحص الأمان + إثباتات حية

### أ. شريط «إتقان تك» — انبنى واشتغل وأثبتّه حيّاً

سطر بأسفل كل متجر: **«تم إنشاء وتصميم وبرمجة هذا الموقع بواسطة إتقان تك»** — الاسم بخط بارز وبلون رابط قالب كل متجر نفسه.

| الإثبات الحي | النتيجة |
|---|---|
| لوحة «شريط التوقيع أسفل المواقع» بصفحة الباقات | ✅ ظهرت، حفظت الاسم والرابط |
| الشريط على متجر بقالب ديوان | ✅ الاسم بلون `#e08a5f` (برتقالي ديوان)، عريض، بالنص |
| الشريط على متجر بقالب سوق نيون | ✅ نفس الجملة بلون وردي نيون — **بيتأقلم مع كل قالب تلقائياً** |
| إطفاء الشريط من اللوحة → تحديث المتجر | ✅ اختفى فوراً |
| تشغيله ثانية | ✅ رجع فوراً |
| هل التاجر بيشوف الإعداد؟ | ❌ أبداً — ما في مفتاح ميزة ولا صلاحية ولا شاشة بلوحة التاجر |

حمايات مدمجة: ما بينحفظ رابط غير http/https، وقاعدة البيانات نفسها بترفض «مفعّل بدون اسم أو رابط»، وكل تغيير مسجّل بسجل العمليات. **الوضع الحالي: مفعّل، الاسم «إتقان تك»، الرابط https://itqantech.com — غيّره من صفحة الباقات وقت ما بدك.**

وأثناء البناء صلّحت خطأين لقيتهم بالفحص الحي (توكن خط وهمي + قاعدة CSS كانت بتبتلع لون الرابط) — الاثنين من أصناف عيوب موثّقة أصلاً بالمشروع.

### ب. تحكّم الأقسام — أثبتّه بالدورة الكاملة

| الخطوة | النتيجة |
|---|---|
| صفحة أقسام التاجر (الوضع الافتراضي) | ✅ ٩ أقسام: إظهار/إخفاء، ترتيب، إعدادات لكل قسم |
| من لوحتك: «من بيعدّل شو» → ترتيب الأقسام → **إدارة المنصة** | ✅ بضغطة وحدة |
| صفحة التاجر بعدها مباشرة | ✅ انقفلت: «هذا الحقل بتعدّله إدارة المنصة» + زر «اطلب تعديل» + عداد الطلبات |
| رجّعتها **التاجر** | ✅ رجعت قابلة للتعديل فوراً |

يعني: بتقدر تعطي التاجر التحكم أو تسحبه منه، لأي ميزة مُدارة، من صفحة صلاحيات حسابه — والتأثير فوري.

### ج. محاكاة تاجر حقيقية

أضفت منتج من لوحة التاجر («سماعات بلوتوث لاسلكية» بـ ١٤٩ ₪) → **ظهر على المتجر فوراً** مع زر «أضف للسلة» والسعر منسّق صح. نموذج المنتج كامل: تركيبات مقاس/لون، سياسات مخزون، حد تنبيه النفاد، حقول SEO.

### د. فحص الأمان

**ثغرة حقيقية وحدة لقيتها وسكّرتها:** مجلد قاعدة بيانات التطوير `.pgdata-dev` (فيه كل بيانات التجار) ما كان بقائمة تجاهل git — كان ممكن ينرفع مع الكود بغلطة. سكّرت الصنف كله (`.pgdata*/`).

فحوصات حية نجحت كلها: رؤوس الحماية CSP + nosniff + منع التأطير موجودة على كل رد · نطاق مجهول → 404 حقيقي بصفحة عربية · زائر بلا جلسة على `/backups` → صفحة الدخول فقط، صفر بيانات · مراجعة الكود: لا حقن HTML خارج المسارين المحميين، لا أسرار بالكود، السجلات بدون بيانات شخصية.

### هـ. النشر على Hostinger — الدليل جاهز: `docs/DEPLOY-HOSTINGER.md`

**القرار المهم:** المنصة بدها **VPS من Hostinger (خطط KVM)** — مش استضافة مشتركة ولا «كلاود». السبب: بدها Docker وصلاحيات root (بوستجرس + ريدس + كادي + عامل خلفي). المقاس المريح: 2 vCPU / 8GB.

**والدومين بينتقل DNS تبعه لـ Cloudflare** (مجاني) حتى لو ظل مسجّل عند Hostinger — شهادة الـ wildcard لنطاقات التجار الفرعية بتنصدر عبر Cloudflare حصراً. الدليل فيه الخطوات كاملة بالترتيب: الجدار الناري، ملف البيئة (مع تحذير تغيير كلمة سر المالك الافتراضية)، الإقلاع، وقائمة «شغل المشغّل» اللي الكود ما بيقدر يعملها عنك.

### بصراحة كاملة — شو المُثبَت وشو لسا

**مُثبَت حيّاً على جهازك:** كل الأسطح، الشريط بالاتجاهين وبقالبين، قفل/فتح الأقسام، منتج جديد بيوصل للمتجر، الحمايات الأساسية، typecheck وlint صفر أخطاء.

**ما بينفحص إلا على سيرفر حقيقي (وأول إقلاع staging بيثبته):** إصدار شهادات wildcard الفعلي، TLS لدومين تاجر مخصص، النسخ الاحتياطي على R2 حقيقي، وصول البريد، وتجربة استرجاع حقيقية. وحزمة الاختبارات الكاملة بدها CI أو PostgreSQL مركّب (قيد جهازك الموثّق).

هذا هو الفرق بين «مكتوب» و«شغّال» — وكل اللي فوق من النوع الثاني.

## 12. جلسة 21 آب مساءً — الأشكال بإيدك + تبسيط الأقسام + عناوين المواقع

### أ. صرت تغيّر شكل أي موقع بنفسك — وأثبتّه حيّاً بثلاث نقلات

لوحة جديدة اسمها **«شكل الموقع»** بأعلى تبويب «محتوى الموقع» لأي حساب: الخمس قوالب (ديوان، سوق نيون، ورشة، بيت، رفّ) مع وصف كل واحد وعيّنات ألوانه، وست خيارات ألوان — الخمس مجموعات الجاهزة **+ «ألوان القالب الأصلية»** ترجّع الموقع لتصميم القالب الأصلي. زر «طبّق الشكل» بيغيّر الموقع **فوراً** قدام الزوار.

| الإثبات الحي على متجر الاناقة | النتيجة |
|---|---|
| الوضع الأصلي | قالب بيت، برتقالي `#E08A5F` على خلفية غامقة |
| اخترت «رفّ» + مجموعة «زيتون» وضغطت طبّق | ✅ المتجر انقلب فوراً: قالب رفّ، أخضر `#3F6212` على فاتح |
| رجّعت «بيت» + «ألوان القالب الأصلية» | ✅ رجع بالضبط متل ما كان `#E08A5F` |

نقطتان مقصودتان: (١) **إنت مش محكوم بباقة التاجر** — بتقدر تحط أي قالب لأي حساب حتى لو باقته على قالب واحد؛ قائمة التاجر نفسه بتظل محكومة بباقته. (٢) لو كان التاجر حاطط ألوان مخصّصة، اللوحة بتحذّرك قبل ما تدوس عليها. وكل تغيير مسجّل بسجل العمليات باسمك مع القيم قبل وبعد.

### ب. صفحة «أقسام الموقع» عند التاجر — صارت تشرح حالها

نفس الوظائف بالضبط (ما انكسر شي)، بس صارت: شرح من ٣ خطوات مرقّمة بأعلى الصفحة (الترتيب = شو بيظهر أول، العلامة = ظاهر/مخفي والإعدادات محفوظة، احفظ = بيبان فوراً) · **أزرار قفز** لإعدادات كل قسم — لأنه في ١٧ نوع قسم والصفحة طويلة · وعلامة **«ظاهر» / «مخفي»** على صندوق إعدادات كل قسم حتى ما يحتار التاجر ليش تعديله مش مبيّن. أثبتّها حيّاً بجلسة تاجر: ٩ أزرار قفز، كلها بتوصل لهدفها.

### ج. عناوين الموقع — دومين مخصص أو عنوان فرعي، والقرار قرارك

لوحة جديدة **«عناوين الموقع»** بصفحة كل حساب عندك بتوريك: العنوان الأساسي المجاني (`اسم-المتجر.دومينك`) اللي بيشتغل دايماً · كل دومين مخصص مربوط وحالته (بانتظار التحقق / تم التحقق / شغّال / فشل) · وجملة بتقلّك وين المفتاح: خيار **«دومين خاص»** بمصفوفة المزايا تحتها مباشرة — شغّلته بيقدر التاجر يربط دومينه، طفّيته ما بيشوف الصفحة أصلاً. وإذا بدك تربطه إنت عنه: «دخول باسم التاجر» وبتعملها من لوحته.

| الإثبات الحي | النتيجة |
|---|---|
| اللوحة على حساب متجر الاناقة | ✅ العنوان الأساسي + «ما في دومين خاص» + حالة المفتاح |
| أضفت `shop.example.com` من لوحة التاجر (بجلسة انتحال) | ✅ انضاف: «بانتظار التحقق» + تعليمات DNS كاملة بالعربي + زر تحقّق |
| رجعت للوحتك | ✅ الدومين ظاهر عندك بحالته «بانتظار التحقق» |
| حذفته | ✅ رجعت اللوحتين على الحالة الفاضية |

مقصود إنه اللوحة عندك **للعرض فقط**: الإضافة والتحقق بيظلوا بمسار واحد (صفحة التاجر) حتى ما يصير عندنا نسختين من قواعد التحقق بتختلفوا مع الوقت.

### د. البوابات

lint نجح من أول مرة (صفر أخطاء). typecheck كشف ٤ أخطاء بملفات اختبار قديمة سببها حقل الشريط `credit` من جلسة أمس — تصلّحت كلها (القيمة الافتراضية: الشريط مطفي). ملاحظة حزمة الاختبارات الكاملة بلا تغيير: بدها CI أو PostgreSQL حقيقي.

## 13. اللوحتان بحلّة جديدة — دارك/لايت مود + اختيار لون

### شو صار

**اللوحتان** — لوحة إدارة المنصة ولوحة التاجر — صار فيهم بأسفل القائمة الجانبية مفتاح صغير أنيق: زر **«الوضع الليلي / النهاري»** بأيقونة قمر/شمس، وتحته **٥ نقاط ألوان** بتغيّر لون اللوحة كلها بكبسة: طيني، زيتوني، بحري، كحلي، كرزي.

نقاط مهمة بالتصميم:

الوضع الليلي **مش رمادي ولا أسود صافي** — درجات بنية دافية بنفس روح اللوحة الفاتحة، والنص أبيض دافي مش أبيض حاد، عشان العين ترتاح فعلاً. كل الألوان الخمسة مدروسة تضمن وضوح القراءة (AA) بالوضعين، بالأزرار والروابط والشارات. والاختيار **بينحفظ لكل متصفح لحاله**: إنت بتختار للوحتك، وكل تاجر بيختار للوحته — وحتى لوحتك ولوحة التاجر مستقلتين (جرّبتها: لوحتك ليلي بحري ولوحة التاجر نهاري بنفس اللحظة). ومع الحفظ، الصفحة بتفتح من أول لحظة بالوضع الصح — بلا وميض أبيض.

وبنفس الشغلة انحسّن ملمس اللوحتين: انتقالات ناعمة عالأزرار والقوائم، ظل خفيف جداً عاللوحات بالوضع الفاتح، وحالة hover واضحة عالحقول والبطاقات.

### الإثباتات الحية

| الفحص | النتيجة |
|---|---|
| كبسة الوضع الليلي بلوحتك | ✅ اللوحة انقلبت غامقة فوراً، خلفية `#171310` |
| اخترت لون «بحري» | ✅ الأزرار والروابط والشارات كلها صارت بحرية |
| تنقلت بين الصفحات وعملت reload | ✅ ظل ليلي بحري — محفوظ من السيرفر نفسه |
| لوحة التاجر بنفس الوقت | ✅ ظلت نهاري — الخياران مستقلان |
| حوّلت لوحة التاجر ليلي + كرزي | ✅ اشتغلت ونحفظت لحالها |
| **مواقع التجار (المتاجر)** | ✅ **ما تأثرت بشي** — ألوانها من قوالبها وبس، متل ما لازم |

### ملاحظة

الدارك مود هذا **للوحات التحكم** (إنت والتجار). ألوان المتاجر نفسها إلها نظامها الخاص اللي بنيناه بجلسة الظهر (القوالب + مجموعات الألوان من لوحتك).
